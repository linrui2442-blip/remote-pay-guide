# Remote Pay Guide — Troubleshooting

## Artifact Not Found During Publish

### Symptom

A publish workflow fails during:

```
actions/download-artifact@v4
```

with:

```
Unable to download artifact(s): Not Found
```

### Cause

Cross-workflow artifact download requires the exact GitHub Actions render workflow Run ID.

The publish workflow uses:

```
source_run_id
```

to locate the artifact generated by the render workflow.

### Resolution

Check:

1. `source_run_id`

It must be the complete GitHub Actions Run ID from the successful render workflow run.

2. Artifact name

It must match the name uploaded by the render workflow.

3. Repository context

The publish workflow must access the same repository that produced the artifact.

### Rule

Do not use:

- workflow number
- job ID
- incomplete Run ID

Always copy the full Run ID from the successful render workflow execution.
