name: R21 Wire Publish Center UI

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/r21-wire-publish-center.yml'

permissions:
  contents: write

jobs:
  wire-publish-center:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Make platform publish readiness UI truthful
        run: python scripts/r21_fix_platform_readiness_ui.py

      - name: Commit App readiness UI
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add os/frontend/src/App.jsx
          git commit -m 'Show truthful live publish readiness in OS UI'
          git pull --rebase origin main
          git push origin HEAD:main
